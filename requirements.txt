# Fire-Protection-Optimization — Python dependencies
# Install with: pip install -r requirements.txt

# MILP solver (ships with CBC binary — no separate install needed)
pulp==3.3.2

# Data loading and preprocessing
pandas==3.0.3

# Interactive HTML maps
folium==0.20.0

# Geospatial data and OSM boundary fetching
geopandas==1.1.3
osmnx==2.1.0
odfpy==1.4.1

# Geometry primitives (pulled in by geopandas/osmnx, pinned for reproducibility)
shapely==2.1.2
